---
title: Calendar
description: The calendar view displays features on a grid calendar. Specifically it shows the end date of each feature, and groups features by day.
icon: CalendarRange
dependencies:
  - https://dndkit.com/
  - https://jotai.org/
  - https://date-fns.org/
  - https://lucide.dev/
installer: calendar
---

## Features

- Features are grouped by day
- Features are color-coded by their status
- Features are truncated if there are too many to fit in the grid cell
- Selectable date range
- Pagination of dates
- Internationalization through `locale` prop
- Pass `currentDate` to the Provider override the calendar's initialization date (default is new Date()
- Pass `showCurrentDate` and `currentDateClassName` to the CalendarBody to highlight the current date.

## Examples

### Without a date picker

<Preview path="calendar-headless" />
